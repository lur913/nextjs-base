import type { Metadata } from "next";
import "./globals.css";


export const metadata: Metadata = {
  title: "根布局",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`antialiased w-[800px] mx-auto p-4`}
      >
        <div className="text-blue-600">这是根布局</div>
        {children}
      </body>
    </html>
  );
}
