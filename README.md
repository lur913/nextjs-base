## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## practise content
- 尝试了 react-hook-form 的基本使用方法
- 尝试了 zod 和 react-hook-form 的结合使用

- 尝试 shadcn 的 react-hook-form 表单完整示例；
后面使用，主要使用这个表单，可以将以下练习当作是
完整的参考：
```
src\app\react-hook-form\page.tsx
```

- react-hook-form 和 server actions 的最佳实践：
```
src\app\rhf-sa\page.tsx
```